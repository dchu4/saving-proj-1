<%= form_for @component do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <br>

  <%= f.label :title %>
  <%= f.text_field :title %>

  <br>

  <%= f.label :body %>
  <%= f.text_area :body, class: 'summernote' %>

  <%= f.submit 'Save' %>

<% end %>
